name: Keep IDX Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping IDX
        run: |
          curl -s https://studio.firebase.google.com/vps123-07155922 || true
